services:
  - type: web
    name: open-id-connect-sso-sample
    env: node
    region: oregon
    plan: free
    buildCommand: pnpm install && pnpm run build
    startCommand: pnpm run start
    envVars:
      - key: SESSION_SECRET
        generateValue: true
      # ★ Set the following manually in Render Dashboard
      # BASE_URL: The URL Render assigns after deployment (e.g. https://your-service.onrender.com)
      # CLIENT_ID: Client ID to register in Shopify
      # CLIENT_SECRET: Client secret to register in Shopify
      # SHOPIFY_API_KEY: API key (client_id) of your Shopify app — used for Admin API OAuth
      # SHOPIFY_API_SECRET: API secret key of your Shopify app — used for session token verification and OAuth HMAC
